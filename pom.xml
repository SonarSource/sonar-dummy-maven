<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.sonarsource.parent</groupId>
    <artifactId>parent</artifactId>
    <version>67.0.0.1948</version>
  </parent>

  <groupId>com.sonarsource.dummy</groupId>
  <artifactId>dummy-maven</artifactId>
  <version>16.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>SonarSource :: dummy-maven</name>
  <inceptionYear>2014</inceptionYear>

  <licenses>
    <license>
      <name>SonarSource</name>
    </license>
  </licenses>

  <properties>
    <sonarVersion>5.1</sonarVersion>
    <sonar.organization>sonarsource</sonar.organization>
    <sonar.projectKey>SonarSource_sonar-dummy-maven-enterprise</sonar.projectKey>
    <artifactsToPublish>${project.groupId}:sonar-dummy-maven-plugin:jar,${project.groupId}:${project.artifactId}:json:cyclonedx</artifactsToPublish>
    <gitRepositoryName>sonar-dummy-maven-enterprise</gitRepositoryName>
  </properties>

  <modules>
    <module>sonar-dummy-maven-plugin</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>${project.groupId}</groupId>
        <artifactId>dummy-frontend</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>org.codehaus.sonar</groupId>
        <artifactId>sonar-plugin-api</artifactId>
        <version>${sonarVersion}</version>
        <scope>provided</scope>
      </dependency>
      <dependency>
        <groupId>junit</groupId>
        <artifactId>junit</artifactId>
        <version>4.13.2</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.mockito</groupId>
        <artifactId>mockito-all</artifactId>
        <version>1.10.19</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>org.easytesting</groupId>
        <artifactId>fest-assert</artifactId>
        <version>1.4</version>
        <scope>test</scope>
      </dependency>
      <dependency>
        <groupId>com.sonarsource.plugins.license</groupId>
        <artifactId>license-api</artifactId>
        <version>2.9</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <issueManagement>
    <system>JIRA</system>
    <url>https://jira.sonarsource.com/browse/TP</url>
  </issueManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.cyclonedx</groupId>
        <artifactId>cyclonedx-maven-plugin</artifactId>
        <version>2.8.1</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>makeAggregateBom</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <projectType>library</projectType>
          <schemaVersion>1.3</schemaVersion>
          <includeBomSerialNumber>true</includeBomSerialNumber>
          <includeCompileScope>true</includeCompileScope>
          <includeProvidedScope>true</includeProvidedScope>
          <includeRuntimeScope>true</includeRuntimeScope>
          <includeSystemScope>true</includeSystemScope>
          <includeTestScope>false</includeTestScope>
          <includeLicenseText>false</includeLicenseText>
          <outputFormat>all</outputFormat>
          <outputName>bom</outputName>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>
